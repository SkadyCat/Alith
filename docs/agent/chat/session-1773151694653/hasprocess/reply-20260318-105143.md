# 爱丽丝回复

**时间**: 2026/3/18 10:51:43

## 任务完成：pyagentTaskPrefix 可选文件限制为 agent/setting

已修改 `public/app.js` 中的 `loadTaskPrefixDocs()` 函数：

**变更内容**：
- `fillPrefixSel(selId)` → `fillPrefixSel(selId, filterPrefix)`，新增过滤参数
- `agentTaskPrefix`（旧版 Agent）：保持不变，仍显示所有文档
- `pyagentTaskPrefix`（PyAgent）：仅显示 `agent/setting/` 目录下的文件
  - 当前可选：`pyagent-thinking`、`thinking`

选项文本也去掉了路径前缀（`agent/setting/`），只显示纯文件名，更简洁。

热重载已生效，刷新页面即可看到效果。
