# 爱丽丝回复

## 任务完成：session_doc 文档归档规则

### 已完成

1. **创建目录** docs/session_doc/ + README.md（说明目录结构）

2. **修改 docs/agent/setting/thinking.md**：在「输出格式规范」第 0 条添加强制规则：
   > 所有 /open/submit 提交的文档，ilename 必须以 session_doc/\session-1773151694653/ 为前缀

3. **修改 docs/agent/setting/pyagent-thinking.md**：同步相同规则

### 效果

thinking 模式下产出的文档会自动存放到：
```
docs/session_doc/{session名称}/文档名.md
```
例如当前会话：docs/session_doc/session-1773151694653/report.md

目录不存在时，/open/submit 路由会自动创建（mkdirSync recursive）。
