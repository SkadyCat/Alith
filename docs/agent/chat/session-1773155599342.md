
> 💬 **[用户留言]**
>
> web工具帮我搜索整理一下现在外网比较火热的 UEEditor 的mcp工具。

● web_fetch: https://www.google.com/search?q=Unreal+Engine+MCP+Model+Context+Protocol+tools+2025
  └ Failed to simplify HTML to markdown. Here is the raw content:

● web_fetch: https://github.com/search?q=unreal+engine+mcp+editor&type=repositories&sort=stars
  └ Contents of https://github.com/search?q=unreal+engine+mcp+editor&type=reposit...

● web_fetch: https://github.com/Natfii/ue5-mcp-bridge
  └ Contents of https://github.com/Natfii/ue5-mcp-bridge:

● web_fetch: https://github.com/appleweed/UnrealMCPBridge
  └ Contents of https://github.com/appleweed/UnrealMCPBridge:

● web_fetch: https://github.com/Italink/UnrealClientProtocol
  └ Contents of https://github.com/Italink/UnrealClientProtocol:

● web_fetch: https://github.com/Natfii/UnrealClaude
  └ Contents of https://github.com/Natfii/UnrealClaude:

● web_fetch: https://github.com/bradenleague/UE-Agent-Asset-Toolkit
  └ Contents of https://github.com/bradenleague/UE-Agent-Asset-Toolkit:

Now I have enough data. Let me compile the document and submit it to the docs service.

● Submit UE MCP tools report to docs service
  $
  $content = @'
  # 🎮 UE Editor MCP 工具全景报告（2026年3月）

  > 整理时间：2026-03-10 | 来源：GitHub 实时检索...
  └ 233 lines...

● Test if docs service is reachable
  $
  [System.Net.WebRequest]::DefaultWebProxy = $null
  $testResp = Invoke-RestMethod -Uri "http://localhost:7439/open/study" -Method GET -ErrorAction
   SilentlyContinue
  $testResp | ConvertTo-Json -Depth 2...
  └ 8 lines...

● Test service connectivity via Python
  $
  # Try Python approach instead
  python -c "
  import urllib.request
  import json...
  └ 17 lines...

● Test with python3
  $
  # Try Python3
  python3 -c "import urllib.request; req =
  urllib.request.Request('http://localhost:7439/open/study'); print(urllib.request.urlopen(req,
  timeout=5).read()[:100])"...
  └ 8 lines...

● Check if port 7439 is listening
  $
  # Check if there's a node.js server running
  netstat -ano | findstr ":7439" | head -5

  └ 7 lines...

● Check if port 7439 is listening
  $
  Get-NetTCPConnection -LocalPort 7439 -ErrorAction SilentlyContinue
  if (!$?) { Write-Host "Port 7439 not listening" }

  └ 10 lines...

● Test with curl.exe
  $
  # Try with curl.exe (built-in Windows)
  $response = curl.exe -s -X GET http://localhost:7439/open/study --max-time 10
  Write-Host $response.Substring(0, [Math]::Min(200, $response.Length))

  └ 12 lines...

● Test with HttpClient
  $
  # Try using HttpClient with a shorter timeout and proper UTF-8 handling
  Add-Type -AssemblyName System.Net.Http
  $client = New-Object System.Net.Http.HttpClient
  $client.Timeout = [TimeSpan]::FromSeconds(30)...
  └ 15 lines...

● Test with node fetch
  $
  # Try with node.js fetch
  node -e "
  fetch('http://localhost:7439/open/study')
    .then(r => r.json())...
  └ 11 lines...

● Find node executable
  $
  # Check where node is available
  Get-Command node* -ErrorAction SilentlyContinue | Select Name, Source
  Get-ChildItem "G:\GameExPro3\Alith\node_modules\.bin\node.exe" -ErrorAction SilentlyContinue

  └ 1 line...

● Find node in PATH
  $
  # Check PATH for node
  $env:PATH -split ";" | Where-Object { $_ -like "*node*" -or (Test-Path "$_\node.exe") }

  └ 1 line...

● Check running server processes
  $
  # Check if the server process is actually running
  Get-Process | Where-Object { $_.Name -match "node|server" } | Select Name, Id, CPU | Format-Table

  └ 13 lines...

● Get node.exe path from running process
  $
  # Find where node.exe is from one of those processes
  $proc = Get-Process -Id 30900
  $proc.Path

  └ 2 lines...

● Test docs service with node http
  $
  $node = "C:\Program Files\nodejs\node.exe"

  # First try a simple GET to verify service connectivity
  & $node -e @"...
  └ 17 lines...

● Submit document via node http
  $
  $node = "C:\Program Files\nodejs\node.exe"

  $content = @'
  # 🎮 UE Editor MCP 工具全景报告（2026年3月）...
  └ 208 lines...

文档已成功写入！现在进入 POLL 等待模式。

📊 上下文进度：约 18%（本轮对话内容 + 工具文档系统设定）

---

**任务执行完毕，整理结果如下：**

共找到 **9 个** UE Editor MCP 工具，文档已保存至 `UE/UEEditor-MCP工具全景报告.md`

🏆 **排行榜亮点**：
- ⭐252 **UnrealClaude** — Claude Code CLI 直接嵌入 UE 5.7
- ⭐40 **UnrealMCPBridge** — Fab 商店可直装，开放完整 Python API  
- ⭐15 **UnrealClientProtocol** — 反射驱动原子协议（国人项目 Italink）
- ⭐12 **ue5-mcp-bridge** — 兼容所有 MCP 客户端
- ⭐10 **UE-Agent-Asset-Toolkit** — 无需开编辑器的离线资产分析

**核心趋势**：MCP 已成为 UE AI 集成的新标准协议，从简单问答演进为直接操控编辑器的 Agent 工作流。

● Enter POLL waiting mode
  $
  $pollCount = 0
  $maxPoll = 100
  $ctxProgress = 18
  $statusFile = "G:\GameExPro3\Alith\runtime\poll_status.md"...
  └ 105 lines...

进入 POLL 模式，等待下一个任务指令。


> 💬 **[用户留言]**
>
> 我希望的工作流程包括创建资产，创建wbp，创建动画，创建蓝图等等。



> 退出码: null
