# 回复

还没完全下好。

**进度：**
- ✅ transformer（主扩散模型）：4767MB + 645MB = **完成**
- ✅ vae（视频编码器）：484MB = **完成**
- ✅ tokenizer / scheduler = **完成**
- ❌ text_encoder（T5文本编码器）：5个shard共~22GB = **下载中**

之前下载中途报错（HTTP连接被关闭），现在已重新启动后台下载（PID 71580），正在续传5个文件。

下载完成后会自动可用，无需任何操作。
